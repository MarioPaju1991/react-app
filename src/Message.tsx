
// PascalCasing
function Message() {
  // JSX: JavaScript XML
  // we can use if statements in JSX
  const name = 'Mario';
  if (name)
    return <h1>Hello {name}!</h1>;
  return <h1>Hello world!</h1>;
}

export default Message;
